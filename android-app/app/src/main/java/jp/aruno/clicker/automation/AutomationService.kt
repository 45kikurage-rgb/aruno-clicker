package jp.aruno.clicker.automation

import jp.aruno.clicker.overlay.AutomationOverlayService

/** Manifest entry point kept in the automation package for the UI's explicit service contract. */
class AutomationService : AutomationOverlayService()
